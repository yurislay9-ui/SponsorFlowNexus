# GitHub Actions Workflow - Android CI/CD
# Compila el APK automáticamente

name: Android Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout del código
      - name: Checkout code
        uses: actions/checkout@v4
      
      # 2. Checkout del submodule de WhatsApp si es necesario
      - name: Setup Android SDK
        uses: android-actions/setup-android@v2
      
      # 3. Configurar Java JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
      
      # 4. Entrar al directorio de WhatsApp
      - name: Build from WhatsApp directory
        run: |
          cd WhatsApp
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon
      
      # 5. Verificar que el APK se generó
      - name: Verify APK exists
        run: |
          ls -la WhatsApp/app/build/outputs/apk/debug/
      
      # 6. Subir APK como artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: WhatsApp/app/build/outputs/apk/debug/*.apk
          retention-days: 30

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: debug-apk
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: *.apk
          name: Nexus Release
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
